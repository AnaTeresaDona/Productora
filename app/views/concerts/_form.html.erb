<%= form_with(model: concert, local: true) do |form| %>
  <% if concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
        <% concert.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :place, 'Lugar' %>
    <%= form.text_field :place %>
  </div>

  <div class="field">
    <%= form.label :participants, 'Asistentes' %>
    <%= form.number_field :participants %>
  </div>

  <div class="field">
    <%= form.label :date, 'Fecha' %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :duration, 'minutes'  %>
    <%= form.number_field :duration%>
  </div>

  <div class="field">
    <%= form.label :group_id, 'Grupo' %>
    <%=form.select :group_id, options_from_collection_for_select(@groups, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
